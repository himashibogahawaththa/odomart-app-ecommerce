<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".views.SellerCardFragment">

    <ImageView
        android:id="@+id/iv_sellerCard"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/seller_card"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <Button
        android:id="@+id/buyer_Btn_sCard"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:layout_marginTop="10dp"
        android:text="@string/buyer"
        android:textAllCaps="false"
        style="?android:attr/borderlessButtonStyle"
        app:backgroundTint="@color/blue_card"
        app:layout_constraintStart_toStartOf="@id/iv_sellerCard"
        app:layout_constraintTop_toTopOf="@id/iv_sellerCard" />

    <Button
        android:id="@+id/seller_Btn_sCard"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:layout_marginTop="10dp"
        android:text="@string/seller"
        android:textAllCaps="false"
        android:textColor="@color/white"
        style="?android:attr/borderlessButtonStyle"
        app:backgroundTint="@color/blue_card"
        app:layout_constraintEnd_toEndOf="@id/iv_sellerCard"
        app:layout_constraintTop_toTopOf="@id/iv_sellerCard" />

    <TextView
        android:id="@+id/buyer_content"
        android:layout_width="225dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="125dp"
        android:text="@string/lorem"
        android:textColor="@color/white"
        android:textSize="15sp"
        android:textStyle="bold"
        app:fontFamily="@font/inter"
        android:lineSpacingExtra="5sp"
        android:letterSpacing="0.125"
        android:gravity="center"
        app:layout_constraintEnd_toEndOf="@+id/iv_sellerCard"
        app:layout_constraintStart_toStartOf="@+id/iv_sellerCard"
        app:layout_constraintTop_toTopOf="@id/iv_sellerCard" />

</androidx.constraintlayout.widget.ConstraintLayout>